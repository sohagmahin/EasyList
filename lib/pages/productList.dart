import 'package:flutter/material.dart';

class productListPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(child: Text('My products page'),);
  }
}